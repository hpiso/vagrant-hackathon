<div class="row">
    <div class="row">
        {% for image in images %}
            {% if image.principal  %}
                <div class="col-lg-12">
                    <img src="{{ asset('assets/img/station/'~image.url) }}" alt="">
                </div>
            {% endif %}
        {% endfor %}
    </div>
    <div class="row">
        {% for image in images %}
            {% if image.principal %}
            {% else %}
                <div class="col-lg-4">
                    <img src="{{ asset('assets/img/station/'~image.url) }}" alt="">
                </div>
            {% endif %}
        {% endfor %}
    </div>
</div>


