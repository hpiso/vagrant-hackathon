
    <div class="row">
        <div class="col-lg-12">
            <h3>Your itinerary</h3>
            <p>1) First, you need to find <span style="font-weight: bold"> {{ station }} </span>station</p>
        </div>
        {% for image in images %}
            {% if image.principal  %}
                <div class="col-lg-12">
                    <img src="{{ asset('assets/img/station/'~image.url) }}" alt="" class="img-responsive">
                </div>
            {% endif %}
        {% endfor %}
    </div>

    <div class="row" style="margin-top: 10px">
        <div class="col-lg-12">
            <h5>Places nearby (help) :</h5>
        </div>
        {% for image in images %}
            {% if image.principal %}
            {% else %}
                <div class="col-lg-4">
                    <img src="{{ asset('assets/img/station/'~image.url) }}" alt="" class="img-thumbnail">
                </div>
            {% endif %}
        {% endfor %}
    </div>


